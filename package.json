{
  "name": "dmx-on-demand-controller",
  "version": "0.0.1",
  "description": "controller for dmx on demand device",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "AGPL-3.0",
  "dependencies": {
    "serialport": "^6.2.2",
    "ws": "^6.0.0"
  }
}
